# Инструкция по использованию Sanity CMS

## Что было настроено

✅ Установлены все необходимые пакеты
✅ Создана конфигурация Sanity Studio
✅ Настроены схемы данных для всего контента сайта
✅ Создан `.env.local` с учетными данными
✅ Sanity Studio запущен на http://localhost:3333

## Как запустить админку

### 1. Запустить Sanity Studio (админка)
```bash
npm run studio
```
Откроется на **http://localhost:3333**

### 2. Запустить основной сайт
```bash
npm run dev
```
Откроется на **http://localhost:3001**

## Структура контента в Sanity

В админке доступны следующие типы контента:

### 📋 Услуги (service)
- Название
- Краткое описание
- Полное описание
- Возможности (список)
- 3 ценовых тарифа (Базовый, Профессиональный, Корпоративный)

### ⭐ Преимущества (advantage)
- Название
- Описание
- Иконка (SVG текст)

### 📰 Новости (news)
- Заголовок
- Дата
- Описание

### ❓ FAQ (faq)
- Вопрос
- Ответ
- Порядок отображения

### 📡 Операторы связи (telecomOperator)
- Название
- Логотип (загрузка изображения)
- Порядок отображения

### ⚙️ Настройки сайта (siteSettings)
Единственный документ, содержит:
- **Статистика дата-центра**: SLA, мощность, количество стоек и т.д.
- **Контактная информация**: телефоны, email, адрес, координаты, часы работы

## Как начать использовать

### Шаг 1: Открыть админку
1. Откройте http://localhost:3333 в браузере
2. Войдите с помощью своего аккаунта Sanity (используйте тот же аккаунт, с которого создавали проект)

### Шаг 2: Заполнить контент

#### Настройки сайта (siteSettings)
1. В левом меню выберите "Настройки сайта"
2. Заполните все поля со статистикой дата-центра
3. Заполните контактную информацию
4. Нажмите "Publish"

#### Операторы связи (telecomOperator)
1. В левом меню выберите "Оператор связи"
2. Создайте новый документ (Create new)
3. Загрузите логотипы операторов (те, что мы конвертировали в WebP)
4. Установите порядок отображения (order): 1, 2, 3, 4...
5. Опубликуйте каждый документ

#### Услуги (service)
1. Создайте 3 документа:
   - ID: "colocation", Название: "Colocation"
   - ID: "cloud", Название: "Cloud Services"
   - ID: "additional", Название: "Дополнительные услуги"
2. Для каждой заполните описания и тарифы
3. Опубликуйте

#### Преимущества, Новости, FAQ
Аналогично создавайте документы и публикуйте.

### Шаг 3: Подключить Sanity к сайту

Сейчас сайт использует `mockData.ts`. Нужно будет обновить компоненты, чтобы они получали данные из Sanity.

#### Пример использования в коде:

```typescript
import { getServices, getSiteSettings } from '@/lib/sanity'

export default async function Page() {
  const services = await getServices()
  const settings = await getSiteSettings()

  return (
    <div>
      {services.map(service => (
        <div key={service.id}>
          <h2>{service.title}</h2>
          <p>{service.shortDescription}</p>
        </div>
      ))}
    </div>
  )
}
```

#### Для изображений:

```typescript
import { urlFor } from '@/lib/sanity'

// В компоненте:
<img
  src={urlFor(operator.logo).width(200).url()}
  alt={operator.name}
/>
```

## Доступные функции API

В файле `lib/sanity.ts` доступны следующие функции:

- `getServices()` - получить все услуги
- `getAdvantages()` - получить все преимущества
- `getNews()` - получить все новости (сортировка по дате)
- `getFAQ()` - получить все вопросы FAQ
- `getTelecomOperators()` - получить всех операторов связи
- `getSiteSettings()` - получить настройки сайта
- `urlFor(imageRef)` - построить URL для изображения

## Передача заказчику

1. **Создайте проект на Sanity.io для заказчика** (бесплатный план)
2. **Пригласите заказчика**: В настройках проекта на Sanity.io → Members → Invite
3. **Дайте инструкцию**: Покажите как открыть http://localhost:3333 и редактировать контент
4. **Опционально**: Можно развернуть Sanity Studio как отдельный сайт (например, на Vercel), чтобы не запускать локально

### Деплой Sanity Studio на отдельный URL

```bash
npx sanity deploy
```
Это создаст URL вида: https://yourdc.sanity.studio

## Важно

- Санити бесплатен до 3 пользователей
- Все изображения автоматически оптимизируются Sanity CDN
- Контент обновляется в реальном времени
- Есть контроль версий (история изменений)

## Учетные данные

- **Project ID**: 6uy69xt8
- **Dataset**: production
- **API Token**: Хранится в `.env.local`
- **Admin URL**: http://localhost:3333 (локально) или deploy на Sanity

## Поддержка

Документация Sanity: https://www.sanity.io/docs
